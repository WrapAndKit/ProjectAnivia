namespace Assets.Logic.Other {
    /// <summary>
    /// Game modes: 
    /// * Playing mode for playing
    /// * Stop mode for pause
    /// * View mode for painter
    /// </summary>
    public enum EGameMode {
        Playing,
        View
    }
}
